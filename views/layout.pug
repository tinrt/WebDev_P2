doctype html
html
    head
        title Contact List
        link(rel='stylesheet', href='/styles.css')
    body
        nav
            if !user
                a(href='/login') Sign in |
                a(href='/signup') Sign up
            else
                span #{user.Username} 
                a(href='/logout') Logout
        block content
